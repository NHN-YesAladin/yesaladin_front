eureka:
  client:
    enabled: false

spring:
  application:
    name: yesaladin-front
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:h2:mem:/test
    username: sa
    password:

yesaladin:
  gateway: http://api.yesaladin.shop:8072/yesaladin-shop